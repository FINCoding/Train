class BinaryTreeNode:
    count = 0
    def add(self, value):
        if head:
            addTo(head, value)
        else:
            head = BinaryTreeNode(value)
    
    def addTo(self, node, value):
        